using UnityEngine;
using System.Collections;

public class camera_rotation : MonoBehaviour {

	// Use this for initialization
	void Start () {
        Cursor.visible = false;
    }
	
	// Update is called once per frame
	void Update () {
      //float mouse_y = Input.GetAxis("Mouse Y") * 0.2f ;
      //transform.rotation = transform.rotation * Quaternion.Euler(-1 * mouse_y, 0, 0);
        

    }
}
